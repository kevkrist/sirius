# PR-3 merge-gate evidence index (design `groupjoin-framework-design.md` section 9, PR-3 row)

All artifacts live under this `pr3/` scratchpad directory unless noted. Baseline = branch
`feat/groupjoin-framework` HEAD 5d1e5f08 (PR-1+PR-2 merged); candidate = the PR-3 working tree
built at `build/release/`.

| gate | status | artifact |
|---|---|---|
| scattered-atomic microbenchmark (section 4.4 prerequisite) | DONE (pre-archived) -- GB300: 21.2 G scattered atomicAdds/s over a 3.2-4 GB device array; a 6 B-row unfiltered dense pass costs ~565 ms (alone exceeding today's whole q17), while the 16 M-row filtered regime costs ~1.5 ms -- confirming section 5.1's estimate is conditional on the membership-filtered regime and that the SF1000 one-shot fusion must decline until PR-5 streams the counted side | `../pr3-prep/atomic-bench-results.txt` (source `../pr3-prep/atomic_bench.cu`) |
| count-kernel SASS parity vs original baseline | PASS -- harness self-test PASS (baseline-vs-baseline clean, single-instruction mutation detected); 24/24 baseline count-kernel roles instruction-identical across 8 archs; 81 candidate-only value-bundle roles (whitelisted new kernels, unchanged from PR-2 -- PR-3 touches no `.cu`). Re-run on the final default-on binary (`sass-final.txt`), same verdict | `sass-parity.md` (baseline `../baseline/sass-full.txt`, candidates `sass-new.txt` pre-flip / `sass-final.txt` final, harness `../pr2/sass_parity.py`) |
| knob-off planning bit-identical | PASS -- evidence form: source inspection (rung P0 verbatim and first inside its own `enable_dense_count_join` guard; rung P1 strictly behind `enable_group_join`) plus a full 22-query plan-render diff of the PR-3 knob-off SF1000 run against the archived PR-1 kit-B run (whose plans were verified against the pre-refactor baseline): plan render, fusion-decision, runtime-strategy, and reservation lines identical for q1-q22 (q17: 169 normalized plan lines, unfused), totals 8.6761 s vs 8.6116 s (-0.74%, noise). HEAD was not rebuilt (accepted alternative per the gate definition); [group_join] detection negatives cover the knob-off planner decisions at unit level | `knoboff-vs-pr1B-plan-parity.md` (A = `tpch_20260825_014000_gj_pr1_B`, B = `tpch_20260825_160318_gj_pr3_sf1000_A`) |
| SF100 kit A/B (knob off/on) | PASS -- same final binary, stock yaml vs `sirius-groupjoin-on.yaml`. Fusion: q17-B logs `Fusing INNER AVG into GROUP_JOIN ... membership publication installed` and the plan render carries `GROUP_JOIN` with preserved/counted FULL ports; no other query fuses (q13's P0 marker identical in A and B; fusion-marker scan over all 22 per-query logs). Regime: sparse (`INNER AVG sparse path: state bytes 319969152, input bytes 7320216`) -- the preserved-key membership filter fired and collapsed the counted lineitem scan to 7.3 MB, exactly the design's filtered regime. Plans: q1-q16, q18-q22 renders identical A/B (only q17 differs, by the fusion); all 22 `result.txt` byte-identical A/B. Runtimes: suite total 3.2199 s (A) vs 3.2287 s (B), +0.27%; q17 best-of-3 showed +19% inside a suite where UNCHANGED queries swing -22%..+16%, so q17 was re-measured at 9 iterations: best 0.1385 s (off) vs 0.1344 s (on), post-warmup mean 0.1505 vs 0.1542 -- improves on best-of, neutral within noise | `sf100-plan-parity.md`; run dirs `tpch_20260825_154946_gj_pr3_sf100_A2`, `tpch_20260825_155214_gj_pr3_sf100_B2`; 9-iteration reruns `tpch_*_gj_pr3_sf100_q17_offx9` / `_onx9`; raw logs `kit-sf100-A2.log`, `kit-sf100-B2.log` |
| SF1000 kit A/B (knob off/on) | PASS -- same final binary. q17-B fusion DECLINES by the counted-side byte gate with log evidence (`GROUP_JOIN INNER fusion declined: counted child estimate 71991962640 bytes (5999330220 rows) exceeds the counted-side byte gate 11173625856`); no INNER fusion in either run; all 22 plan renders/fusion/strategy/reservation lines identical A/B (plan_parity PASS); all 22 `result.txt` identical; totals 8.6116 s (A) vs 8.5976 s (B), -0.16% -- leave-one-out discipline (within +-1.2%) satisfied | `sf1000-plan-parity.md`; run dirs `tpch_20260825_160318_gj_pr3_sf1000_A`, `tpch_20260825_160803_gj_pr3_sf1000_B`; raw logs `kit-sf1000-A.log`, `kit-sf1000-B.log` |
| focused test tags | PASS -- `[group_join]` 70 cases / 62103 asserts, `[config]` 65 / 710, `[tier_narrowing_policy]` 12 / 105, `[compressed_schema_propagation]` 11 / 222; re-run green on the final default-on binary | `test-*-flip.log` (final binary), earlier runs `test-group_join-*.log`, `test-config*.log`, `test-tier_narrowing_policy.log`, `test-compressed_schema_propagation.log` |
| pre-commit | PASS on every changed file (clang-format reformatted once mid-stream; all kit/SASS/test gates were taken on post-format binaries; final tree passes all hooks) | |
| default flip (`enable_group_join` -> true) | DONE -- flipped after every gate above passed; rebuilt; `[group_join]`+`[config]` (and the two schema tags) re-run green; SASS parity re-verified on the flipped binary. The measured kit gates pinned the knob explicitly in YAML on the pre-flip binary, so the flip changes only the compile-time default | `test-*-flip.log`, `sass-final.txt` |

The full unit suite remains Kevin-gated (machine policy): `pixi run make test`. The SF100/SF1000 kit
pairs above provide the end-to-end coverage for this PR's pathway.

## Post-review fixes (2026-08-25)

- Reviewer finding (major): `docs/super-sirius/configuration.md:603` listed the
  `enable_group_join` default as `false`, stale from before the gated default flip; code default
  is `true` (`src/include/sirius_config.hpp:103`). Fixed the table cell to `true` (docs-only,
  one line). No binary, test, SASS, or kit gate invalidated; `pre-commit` re-run on the file,
  all hooks pass.
